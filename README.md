# ses-log | dev stage - do not use
```
usage: SESCLI [-h] [--show-config] [--configure] [--delete-resources]

optional arguments:
  -h, --help            show this help message and exit
    --show-config, -s     show current configuration
      --configure, -cfg     setup CLI and resources
        --delete-resources, -dr
```

